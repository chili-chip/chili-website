# chili-website

## Quick start
1. Install Hugo:

    ```sh
    sudo snap install hugo
    ```

2. Install blowfish-tools

    ```sh
    npm i -g blowfish-tools
    ```

3. Run cli tool
    
    ```sh
    blowfish-tools
    ```
